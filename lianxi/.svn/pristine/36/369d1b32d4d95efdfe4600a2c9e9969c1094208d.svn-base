<template>
  <el-container>
    <el-header class="query_headbox">
      <div v-if="!noTitle" class="com-title">
        <span class="ti_text">
          <slot name="title">
            <span class="titlebox">{{title}}</span>
          </slot>
        </span>
      </div>
      <div v-if="!noRetrie" class="retrieval query_head">
        <slot name="retrieve"></slot>
        <inpbox>
          <fel-button class="qh_btn" type="primary" @click="onQuery">查询</fel-button>
          <fel-button class="qh_btn" @click="onReset">重置</fel-button>
        </inpbox>
      </div>
    </el-header>
    <el-main class="padt0 query_main">
      <slot></slot>
    </el-main>
  </el-container>
</template>

<script>
export default {
  props: {
    noTitle: Boolean,
    title: String,
    noRetrie: Boolean
  },
  methods: {
    onQuery() {
      this.$emit("onQuery");
    },
    onReset() {
      this.$emit("onReset");
    }
  }
};
</script>

<style lang='scss'>
.container {
  width: 100%;
  height: 100%;
  display: flex;
  background-color: #fff;
  flex-direction: column;
  position: relative;
  .main {
    flex: 1;
  }
}
</style>
