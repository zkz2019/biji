<!-- 批量授权-->
<template>
  <el-container>
    <el-header class="query_headbox">
      <com-title>{{toParam.alias}}</com-title>
      <retrieval class="query_head">
        <inpbox :inptext="'请输入'">
          <el-input
            clearable
            class="con-search qh_inp"
            v-model="param.search"
            :placeholder="'输入'+getNumber()+'/姓名/卡号进行检索'"
          ></el-input>
        </inpbox>
        <inpbox>
          <el-button @click="cardInput" class="qh_btn" type="primary">查询</el-button>
          <fel-button class="qh_btn" @click="onReset">重置</fel-button>
        </inpbox>
      </retrieval>
    </el-header>
    <el-main class="pad0 query_main">
      <paging-table
        class="tobleList wh100"
        height="100%"
        interface="/auth/cardcenter/cardauth/1/getcardauth"
        @onSelection="onSelection"
        :list="list"
        :refresh="refresh"
        :param="param"
        @onEjectChange="onEjectChange"
      >
        <span class="sli but-blue" v-if="showBtn" @click.stop="onClick('d1')">
          <i class="ficon-batch"></i>批量授权卡
        </span>
      </paging-table>
    </el-main>
    <fel-dialog
      :title="titleList"
      width="75%"
      minWidth="1000px"
      class="grantDialog"
      :close-on-click-modal="false"
      :before-close="beforeClose"
      :visible.sync="dialogVisible"
    >
      <el-container class="dialog-table6 query_main">
        <paging-table
          v-if="grantType == 2"
          key="type2"
          interface="/auth/cardcenter/cardauth/3/getcardbackauthinfo"
          :param="grantParam"
          :refresh="grantRefresh"
          :list="grantList2"
        >
          <span v-for="(v,k) of topButs" :key="k" class="sli but-blue" @click="onClick(v.id, v)">
            <i v-if="v.icon" :class="'ficon-'+v.icon"></i>
            {{v.alias}}
          </span>
        </paging-table>
        <paging-table
          v-else
          key="type1"
          interface="/auth/cardcenter/cardauth/2/getcardauthinfo"
          :param="grantParam"
          :refresh="grantRefresh"
          :list="grantList1"
        >
          <span v-for="(v,k) of topButs" :key="k" class="sli but-blue" @click="onClick(v.id, v)">
            <i v-if="v.icon" :class="'ficon-'+v.icon"></i>
            {{v.alias}}
          </span>
        </paging-table>
      </el-container>
    </fel-dialog>
  </el-container>
</template>

<script>
import Storages from "../../utils/Storage.js"; //缓存工具
import { mapGetters } from "vuex";
export default {
  components: {},
  props: {
    toParam: null,
    showBtn: Boolean,
    topButs: Array
  },
  data() {
    let $this = this;
    return {
      titleList: "卡片授权列表",
      dialogVisible: false,
      grantParam: { sceid: "" },
      grantType: 1,
      grantList1: [
        {
          name: "序号",
          type: "$index",
          width: "60px"
        },
        {
          name: "房间位置",
          prop: "roomlocation"
        },
        {
          width: "80px",
          name: "卡号",
          prop: "cardcode"
        },
        {
          width: "70px",
          name: "卡类别",
          prop: "cardtype"
        },
        {
          width: "80px",
          name: "姓名",
          prop: "personname"
        },
        {
          minWidth: "80px",
          name: this.getNumber(),
          prop: "personcode"
        },
        {
          minWidth: "110px",
          name: "授权开始时间",
          prop: "empsdate"
        },
        {
          minWidth: "110px",
          name: "授权结束时间",
          prop: "empedate"
        },
        {
          minWidth: "92px",
          name: "可刷卡次数",
          prop: "rcusecount"
        },
        {
          minWidth: "92px",
          name: "可用时间段",
          prop: "opentime"
        },
        {
          name: "授权状态",
          prop: "scedstatus",
          template: {
            props: ["scope"],
            computed: {
              name() {
                let value = this.scope.row.scedstatus;
                if (value == "0") {
                  return "正在授权";
                } else if (value == "1") {
                  return "授权成功";
                } else {
                  return "授权失败";
                }
              }
            },
            methods: {
              getClass() {
                let value = this.scope.row.scedstatus;
                if (value == "1") {
                  return "puc-pg";
                } else if (value == "-1") {
                  return "puc-px";
                } else {
                  return "";
                }
              }
            },
            template: `<span :class='getClass()'>{{name}}</span>`
          }
        },

        {
          name: "下发状态",
          template: {
            props: ["scope"],
            methods: {
              getClass() {
                let value = this.scope.row.issend;
                if (value == "成功") {
                  return "puc-pg";
                } else if (value == "失败") {
                  return "puc-px";
                } else {
                  return "";
                }
              }
            },
            template: `<span :class='getClass()'>{{scope.row.issend}}</span>`
          }
        },
        {
          name: "失败原因",
          prop: "scedremark"
        }
      ],
      grantList2: [
        {
          name: "序号",
          type: "$index",
          width: "60px"
        },
        {
          name: "房间位置",
          prop: "roomlocation"
        },
        {
          name: "卡号",
          prop: "cardcode"
        },
        {
          name: "卡类别",
          prop: "cardtype"
        },
        {
          name: "姓名",
          prop: "personname"
        },
        {
          name: this.getNumber(),
          prop: "personcode"
        },
        {
          name: "下发状态",
          template: {
            props: ["scope"],
            methods: {
              getClass() {
                let value = this.scope.row.issend;
                if (value == "下发成功") {
                  return "puc-pg";
                } else if (value == "下发失败") {
                  return "puc-px";
                } else {
                  return "";
                }
              }
            },
            template: `<span :class='getClass()'>{{scope.row.issend}}</span>`
          }
        }
      ],
      grantRefresh: 0,
      dialogEdit: false,
      editData: [],
      param: {
        search: ""
      },
      list: [
        {
          name: "序号",
          type: "$index",
          width: "60px"
        },
        {
          name: "授权日期",
          prop: "scecdate"
        },
        // {
        //   name: "授权id",
        //   prop: "sceid"
        // },
        // {
        //   name: "授权类型",
        //   prop: "specialtype"
        // },
        // {
        //   name: "操作日期",
        //   prop: "scecdate"
        // },
        {
          name: "授权类型",
          prop: "specialtype"
        },
        {
          name: "授权总数",
          prop: "sceallcount",
          template: {
            props: ["scope"],
            methods: {
              isNumber(num) {
                if (num) {
                  return /^[0-9]*$/.test(num) && num != 0;
                }
                return false;
              },
              onClick() {
                let obj = this.scope.row;
                if (obj.sceid) {
                  $this.grantType = obj.specialtype2;
                  if (obj.specialtype2 == 2) {
                    $this.titleList = "卡片取消授权列表";
                  } else {
                    $this.titleList = "卡片授权列表";
                  }
                  $this.grantParam.sceid = obj.sceid;
                  $this.grantRefresh = new Date().getTime();
                  $this.dialogVisible = true;
                }
              }
            },
            template: `<div>
              <span v-if="isNumber(scope.row.sceallcount)"><a class="a-click" @click.stop="onClick">{{scope.row.sceallcount}}</a></span>
              <span v-else>{{scope.row.sceallcount}}</span>
            </div>`
          }
        },

        {
          name: "授权成功数",
          minWidth: "100px",
          prop: "sceokcount"
        },

        {
          name: "授权失败数",
          minWidth: "100px",
          prop: "scenocount"
        },
        {
          name: "指令成功数",
          minWidth: "100px",
          prop: "scezlokcount"
        },
        {
          name: "指令失败数",
          minWidth: "100px",
          prop: "scezlnocount"
        },
        // {
        //   name: "授权数量",
        //   prop: "scecount",
        //   template: {
        //     props: ["scope"],
        //     methods: {
        //       isNumber(num) {
        //         if (num) {
        //           return /^[0-9]*$/.test(num) && num != 0;
        //         }
        //         return false;
        //       },
        //       onClick() {
        //         let obj = this.scope.row;
        //         if (obj.sceid) {
        //           $this.grantType = obj.specialtype2;
        //           if (obj.specialtype2 == 2) {
        //             $this.titleList = "卡片取消授权列表";
        //           } else {
        //             $this.titleList = "卡片授权列表";
        //           }
        //           $this.grantParam.sceid = obj.sceid;
        //           $this.grantRefresh = new Date().getTime();
        //           $this.dialogVisible = true;
        //         }
        //       }
        //     },
        //     template: `<div>
        //       <span v-if="isNumber(scope.row.scecount)"><a class="a-click" @click.stop="onClick">{{scope.row.scecount}}</a></span>
        //       <span v-else>{{scope.row.scecount}}</span>
        //     </div>`
        //   }
        // },
        // {
        //   name: "结束日期",
        //   prop: "oledate"
        // },
        {
          name: "操作人账号",
          minWidth: "100px",
          prop: "userlogin"
        },
        {
          name: "操作人姓名",
          minWidth: "100px",
          prop: "username"
        }
        // {
        //   name: "操作",
        //   width: "100px",
        //   template: {
        //     props: ["scope"],
        //     methods: {
        //       onClick(key) {
        //         let obj = this.scope.row;
        //         $this.inReloadlotcard(obj.specialtype2, obj.sceid);
        //       }
        //     },
        //     template: `<el-button type="text" size="small" @click.stop="onClick">重载授权</el-button>`
        //   }
        // }
      ],
      refresh: 0,
      tableData: [],
      defaultData: {}
    };
  },
  mounted() {
    this.getEject();
  },
  methods: {
    //重置事件
    onReset() {
      this.param.search = "";
      this.cardInput();
    },
    ...mapGetters(["getNumber"]),
    beforeClose() {
      this.dialogVisible = false;
    },
    onSelection(data) {
      this.tableData = data;
    },
    confirm(data) {
      let arr = [];
      if (this.editData instanceof Array) {
        this.editData.forEach(obj => {
          obj["oledate"] = data.edate || "";
        });
        arr = this.editData;
      } else {
        this.editData["oledate"] = data.edate || "";
        arr.push(this.editData);
      }
      this.edit(arr);
    },
    cardInput() {
      this.refresh = new Date().getTime();
    },
    //删除离线授权
    delete(arr) {
      this.$confirm("确定要删除这些授权吗？", "提示", {
        confirmButtonText: "确定",
        cancelButtonText: "取消",
        type: "warning"
      })
        .then(() => {
          this.$ajax(
            "/auth/cardcenter/offlineauth/saveofflineauth/1/deleteofflineauth",
            {},
            "1",
            arr
          )
            .then(res => {
              this.cardInput();
              this.$message({
                message: "删除成功",
                type: "success"
              });
            })
            .catch(err => {
              this.$message({
                showClose: true,
                message: `[${err.resultCode}] ` + err.resultMsg,
                type: "error"
              });
            });
        })
        .catch(() => {});
    },
    //修改
    edit(arr) {
      this.$ajax(
        "/auth/cardcenter/offlineauth/saveofflineauth/1/updateofflineauth",
        {},
        "1",
        arr
      )
        .then(res => {
          this.cardInput();
          this.dialogEdit = false;
          this.$message({
            message: "修改成功",
            type: "success"
          });
        })
        .catch(err => {
          this.$message({
            showClose: true,
            message: `[${err.resultCode}] ` + err.resultMsg,
            type: "error"
          });
        });
    },
    onClick(key, data) {
      if (key == "446") {
        this.submitForm();
      } else if (key == "d1") {
        this.$emit("onClick", key, data);
      }
    },
    submitForm() {
      this.inReloadlotcard(this.grantType, this.grantParam.sceid);
    },
    inReloadlotcard(type, sceid) {
      this.$confirm(
        `确定要重载${type == 2 ? "取消" : ""}卡片的授权吗？`,
        "提示",
        {
          confirmButtonText: "确定",
          cancelButtonText: "取消",
          type: "warning"
        }
      )
        .then(() => {
          let url = "/auth/cardcenter/lotauth/reload/1/reloadlotcardauth";
          if (type == 2) {
            url = "/auth/cardcenter/lotauth/reload/2/reloadlotcardbackauth";
          }
          this.$ajax(url, { sceid }, "1", {}, true)
            .then(res => {
              this.$message({
                message: "重载授权成功",
                type: "success"
              });
              this.grantRefresh = new Date().getTime();
            })
            .catch(err => {
              this.$message({
                showClose: true,
                message: `[${err.resultCode}] ` + err.resultMsg,
                type: "error"
              });
            });
        })
        .catch(() => {});
    },
    onEjectChange() {
      this.$common.onEjectChange(this.list, "plsq385");
    },
    getEject() {
      this.$common.getEject(this, "list", "plsq385");
    }
  }
};
</script>
